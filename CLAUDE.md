# narduk-libs — entry pointer

This repo's operating guidance is in [`AGENTS.md`](./AGENTS.md) in this directory. Claude
does not load `AGENTS.md` automatically the way it loads `CLAUDE.md`, so read it first, in
full, before doing anything else — the same is true for a local Mac session and a cloud
container.

Gates are in AGENTS.md § Validation.
