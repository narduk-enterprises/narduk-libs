{
  "name": "@narduk-enterprises/narduk-ui",
  "version": "0.1.4",
  "private": false,
  "description": "Narduk shared interface library. Ships the Narduk Status Design System token layer and the instruments component family.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/narduk-enterprises/narduk-libs.git",
    "directory": "packages/design/narduk-ui"
  },
  "publishConfig": {
    "access": "restricted",
    "registry": "https://npm.pkg.github.com"
  },
  "sideEffects": [
    "*.css",
    "*.vue"
  ],
  "files": [
    "tokens.css",
    "instruments",
    "_core",
    "README.md"
  ],
  "exports": {
    "./tokens.css": "./tokens.css",
    "./core": "./_core/index.ts",
    "./instruments": "./instruments/index.ts",
    "./instruments/*.vue": "./instruments/*.vue"
  },
  "scripts": {
    "build": "pnpm run typecheck",
    "check:package": "publint --strict && pnpm pack --dry-run",
    "format:check": "prettier --check --log-level warn \"**/*.{ts,vue,json,css,md}\" --ignore-path ../../../.gitignore --ignore-path ../../../.prettierignore",
    "lint": "eslint . --max-warnings 0 --ignore-pattern vitest.config.ts",
    "//lint": "vitest.config.ts is ignored because @vitejs/plugin-vue → vite@7 trips eslint-plugin-import-x@4.17.1 import-x/no-cycle (\"node with invalid interface loaded as resolver\"). narduk-charts gets a root-eslint files override for the same crash; this lane cannot touch eslint.config.mjs.",
    "test:unit": "vitest run",
    "typecheck": "vue-tsc --noEmit --project tsconfig.json"
  },
  "//devDependencies": "happy-dom, @vitejs/plugin-vue, @vue/test-utils, @vue/server-renderer and @types/node are pinned to exact versions already resolved in this workspace's lockfile. A caret range makes pnpm re-resolve the shared entry to the newest satisfying release and re-key or move it for every other package, which is exactly the silent dependency drift narduk-libs#124 warns folds against. Widen them in a deliberate workspace-wide bump, not here.",
  "devDependencies": {
    "@types/node": "22.19.19",
    "@vitejs/plugin-vue": "6.0.7",
    "@vue/server-renderer": "3.5.39",
    "@vue/test-utils": "2.4.11",
    "eslint": "^10.8.0",
    "happy-dom": "20.10.6",
    "prettier": "^3.8.3",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11",
    "vue": "^3.5.39",
    "vue-tsc": "^3.2.5"
  },
  "peerDependencies": {
    "vue": ">=3.5.0"
  },
  "volta": {
    "node": "22.22.3"
  }
}
