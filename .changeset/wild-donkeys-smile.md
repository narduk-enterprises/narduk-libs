---
'@narduk-enterprises/create-narduk-app': patch
---

Update the generator's pinned `@narduk-enterprises/narduk-testkit` version to
the release that adds the console tracker's deterministic telemetry profile.
Generated apps keep today's behavior: the profile is opt-in and the default
stays `'live'`.
