---
'@narduk-enterprises/narduk-mapkit-nuxt': patch
---

Backfill `AppMapKit` to the shared component suite bar: README props, slots,
events and example, plus a happy-dom mount test and a node `renderToString` SSR
proof. NE Base cards wait for #250.
