---
'@narduk-enterprises/narduk-charts': patch
---

Document `NardukScatterChart`, `NardukHistogramChart`, `NardukChartStack` and
`NardukBrandBackdrop` to the suite bar (props, slots, events, one example) and
add mount plus no-DOM SSR coverage so every exported chart component has both.
