---
'@narduk-enterprises/narduk-app-tools': patch
'@narduk-enterprises/create-narduk-app': patch
---

Resolve the active Worker version from provider deployment allocation instead of
versions-inventory list position, page the deployments list the same way as
versions when `result_info` is present, and add bounded preview-alias identity
convergence with aggregated post-convergence diagnostics (narduk-libs#47).
