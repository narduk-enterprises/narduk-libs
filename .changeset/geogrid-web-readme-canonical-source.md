---
'@narduk-enterprises/geogrid-web': patch
---

Point the package README's "Repo" row and `package.json`'s `homepage`/`bugs.url` at this repository (`packages/modules/geogrid-web`) instead of the standalone `narduk-enterprises/GeoGridWeb` repository. That repo is a superseded duplicate pending retirement, not the source; this package's `repository` field already pointed here (operator-portal#510).
