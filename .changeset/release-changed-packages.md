---
'@narduk-enterprises/narduk-mapkit': patch
---

Document the 2.1.0 API surface in `docs/api-2.1.md`: the exports map, the Nuxt
module options and environment variable names, the `<AppMapKit>`
prop/emit/slot/expose contract, the Apple loader and token-refresh contract, the
same-host token route, testable performance budgets, the `./testing` fake, and
the honest list of what remains unverified. Documentation only — no runtime code
changes in this release.
